📅 FIELD NOTES: ENGINEERING SPRINT LOG
Date: June 17, 2026
Engineer: Jaspreet
Project: Kisan Mitra (RiceDoctor-EdgeAI)
🎯 OBJECTIVE OF THE DAY:
Establish the public "Proof of Work" architecture by migrating the core Edge-AI training pipeline from local/Google Colab environments to a public GitHub repository. The goal is to document the "Distance Traveled" from the initial 47% baseline to the 76% production-ready model.
⚠️ CONSTRAINTS & ROADBLOCKS ENCOUNTERED:
The Base64 Output Bloat: Attempted to upload the MobileNetV2_End_to_End_Training.ipynb directly to GitHub. The upload repeatedly failed, throwing a "We can't process this file" error.
Root Cause Analysis: Identified that Jupyter Notebooks save visual outputs (graphs, leaf images) as massive Base64 text strings, which artificially inflated the file size to over 15MB and crashed the constrained mobile browser connection (Moto G85 cast to TV).
API Handshake Failure: Attempted the "Save a copy to GitHub" direct API link from Google Colab, but the mobile browser's background protocols blocked the handshake.
⚙️ ACTIONS TAKEN (THE ENGINEER'S BYPASS):
Output Purge: Executed a hard wipe of all visual outputs (Edit -> Clear all outputs) inside Google Colab to strip the Base64 bloat and reduce the file to pure mathematical logic.
Direct Injection Protocol: Completely bypassed the failing UI upload buttons. Translated the pipeline into pure Python logic, opened GitHub's native text editor, and directly injected the code into a new file: MobileNetV2_End_to_End_Training.py.
📈 RESULTS & COMMITS:
Successfully bypassed hardware upload limits and secured the master training code in version control.
Commit Executed: Add raw Python script for end-to-end MobileNetV2 training pipeline via direct text injection
The pipeline now accurately documents the transition from a noisy/corrupted dataset (47% initial accuracy) to the clean, real-world agricultural dataset resulting in the 76% accuracy baseline.
🔜 NEXT STEPS FOR TOMORROW:
Execute the identical "Direct Injection" protocol for the historical failure/audit files (MobileNetV2_v1_Baseline_47_Acc.py and MobileNetV2_v2_Data_Audit.py) to fully document the Data Science Root Cause Analysis.





THRUSDAY 18 JuNE 

i do train model n find some hamster n Christmas tree in data set n then i delete that data se t n then have to download new data set n check aguan get qrong n then agian change this 
n final at laast get real world messy dataset that is really wantved
n at last trained mobilev2net on dtataset n achieved 76.88% accuracy 
================================================================================
BUG ID: #MOD-OVF-001
TIMESTAMP: 2026-07-09 20:44
ARCHRESS: MobileNetV2 Crop Disease Baseline Training Pipeline
--------------------------------------------------------------------------------
[RAW METRICS]
- Training Accuracy : 0.9413 (94.13%)
- Training Loss     : 2.8700
- Validation Acc    : 0.7689 (76.89%)
- Validation Loss   : 3.0800

[DIAGNOSIS]
Operational telemetry indicates severe model overfitting and parameters memorization. 
The neural network demonstrates high reactivity to rigid structural variations 
contained within the baseline laboratory dataset, resulting in weak generalization 
under out-of-distribution real-world field conditions (Haryana dirt-field matrix).

[FIX PROTOCOL / NEXT SPRINT INTERVENTIONS]
1. HARDENING CONFIG: Halt pure 'Heavy Lift' training loops. Implement explicit 
   System Regularization loops to reduce model over-confidence.
2. REGULARIZATION: Inject L2 weight decay parameters [kernel_regularizer=l2(0.01)] 
   across dense layer arrays to force broader parameter distributions.
3. INFERENCE PATHS: Scale global Dropout thresholds to 0.5 to disable rigid linear 
   pathways and eliminate background noise reactive behaviors.
4. IMAGE PIPELINE: Deconstruct the dataloader; inject cloud-connected 
   ImageDataGenerator pipeline extensions (rotations, brightness scaling, illumination 
   shifts) to force parameter variations against dirt, dynamic sunlight, and lens angles.

[STATUS] ACTIVE HYPERPARAMETER BUG HUNTING ENGAGED -- RUNNING SYSTEM UPDATES
================================================================================

================================================================================
UPDATE ID: #MOD-OVF-001-FIX (MEMORIZATION BYPASS)
TIMESTAMP: 2026-07-10 22:30
ARCHRESS: MobileNetV2 Crop Disease Hardened Training Pipeline
--------------------------------------------------------------------------------
[INTERVENTION EXECUTED]
Successfully deployed system hardening protocols to combat the overfitting and 
memorization loop identified in the baseline model. The neural network was 
previously memorizing background laboratory static. 

[ARCHITECTURAL UPGRADES]
1. EXTREME AUGMENTATION: Rewrote the data loader to force the model to process 
   images at extreme angles (40-degree rotations), variable zoom limits, and 
   shifted brightness ranges [0.7, 1.3] to simulate harsh sunlight and shadows.
2. NETWORK REGULARIZATION: Injected a GlobalAveragePooling2D layer to smash 
   spatial dimensions, followed by a Dense layer constrained by an L2 Weight Decay 
   penalty (0.01) to force broader feature distribution.
3. PATHWAY DESTRUCTION: Implemented an aggressive 50% Dropout layer [Dropout(0.5)] 
   to randomly disable neurons during training, preventing the model from relying 
   on single-pixel shortcut pathways.
4. AUTOMATED FAIL-SAFES: Attached EarlyStopping (patience=4) and ReduceLROnPlateau 
   callbacks to autonomously govern the learning rate and prevent resource burn 
   during the 15-epoch compute sprint.

[STATUS] SYSTEM HARDENED. TRAINING LOOP ENGAGED. AWAITING TELEMETRY.
================================================================================
